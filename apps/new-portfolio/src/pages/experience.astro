---
import { H2, Card } from '@ws-portfolio/design-system';

import jobs from './jobs.json'

let _row = 2;
---

<section
  class="w-full lg:px-auto lg:mx-auto px-6 lg:px-0 py-20 bg-dark-white flex flex-col content-center justify-center items-center text-center"
>
  <H2
    variant="secondary"
    className="mb-10 lg:row-start-1 lg:col-start-1 lg:row-end-2 lg:col-end-3"
  >
    Education & Experience
  </H2>
  <div class="lg:grid lg:grid-cols-2 lg:auto-rows-auto">
    <div class="card relative w-full text-left px-[30px] flex flex-col pt-6 pb-8 gap-y-2 border-l border-blue ws-card last-of-type:border-0 lg:border-l-0 lg:text-right lg:border-r lg:ml-[1px] lg:col-start-1 lg:col-end-2" style="grid-row-start:1; grid-row-end:2; width: 10px;"></div>
    {
      jobs.map(({ title, date, description }, index) => {
        let row = index;
        let classes = [];
        const isEven = index % 2;
        if (isEven == 0) {
          classes.push("lg:col-start-1");
          classes.push("lg:col-end-2");
        } else {
          classes.push("lg:col-start-2");
          classes.push("lg:col-end-3");
        }
        row++;
        return (
          <Card
            title={title}
            date={date}
            description={description}
            className={classes.join(" ")}
            row={row}
            isOdd={!isEven}
          />
        );
      })
    }
  </div>
</section>
